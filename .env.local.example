# Redis/KV Configuration
# For development without Redis, the @vercel/kv will use a mock store
# In production on Vercel, connect your Redis database

# Email Configuration (optional)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# OTP Configuration
NEXT_PUBLIC_ENABLE_EMAIL_OTP=true
NEXT_PUBLIC_ENABLE_PHONE_OTP=false
NEXT_PUBLIC_DEFAULT_CONTACT_METHOD=email

# Other configurations
NODE_ENV=development